import { ThemeSwitcherContainer } from './ThemeSwitcher.container'

describe('ThemeSwitcherContainer', () => {
  test('should be defined', () => {
    expect(ThemeSwitcherContainer).toBeDefined()
  })
})
